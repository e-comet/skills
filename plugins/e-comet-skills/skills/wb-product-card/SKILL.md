---
name: wb-product-card
id: wb-product-card
title: Карточка товара WB
description: "Собирает живые данные карточки товара Wildberries через браузер пользователя: цену, рейтинг, остатки по складам и размерам, склейку артикулов. Используйте, когда пользователь спрашивает про остатки или сток артикула ВБ, наличие по складам или размерам, цену, рейтинг, карточку товара или склейку по nm_id / SKU."
skill_schema_version: 1
introduced_in: "2026.7.0"
updated_in: "2026.7.1"
status: experimental
---

# Карточка товара WB

## Контракт

Скилл получает данные карточек товаров Wildberries (одной или батчем до 50 артикулов) через
браузер пользователя: MCP tool `browser_job` подписывает задание, расширение e-Comet выполняет
запросы к WB, задание передаётся и результаты читаются через postMessage-RPC на вкладке
wildberries.ru (снипеты в `scripts/`, работают из изолированного JS-мира без CDP). Сервер
e-Comet не выполняет запросы к WB и не видит ответы — все данные остаются в браузере
пользователя.

## Требования

- **MCP сервер `e-comet`** — устанавливается автоматически вместе с плагином; при первом
  обращении откроется вход по email (код из письма).
- **Расширение e-Comet** — установлено в браузере, выполнен вход (введён ключ расширения).
- **Вход в Wildberries** — пользователь залогинен на wildberries.ru в этом браузере; задания
  без входа отклоняются (`wb_not_authenticated`).
- **Управляемый браузер** — агент должен уметь открывать вкладки и выполнять JavaScript на
  странице (page.evaluate или аналог). Без выполнения JS результаты прочитать нельзя.

## Как работает

1. Вызовите MCP tool `browser_job`:

   ```json
   { "job_type": "product_card", "articles": [791050753, 913357757] }
   ```

   До 50 артикулов, дубликаты схлопываются. Ответ: `trigger_url`, `job_ids` (один родительский
   `jobId`), `expires_at` (~5 минут — открывайте сразу, не откладывайте).

2. Сразу возьмите Chrome пользователя с расширением, а не встроенный (in-app) браузер агента:

   ```js
   agent.browsers.get("extension")
   ```

   Во встроенном браузере расширения e-Comet нет — задание там не выполнится.

3. Откройте в этом браузере любую вкладку wildberries.ru (достаточно главной
   `https://www.wildberries.ru/`) и передайте задание снипетом `scripts/agent_submit.js`,
   подставив в `__TRIGGER_URL__` значение `trigger_url` **как opaque-строку**: ровно как
   вернул tool — не перепечатывайте, не пересобирайте, не редактируйте и не переносите через
   многострочный текст (пробельные символы внутри = значение повреждено, запросите новый
   `browser_job`). Ответ `{ jobIds }` — задание принято, навигация на `trigger_url` не нужна.
   Ответ `{ error }` — это ошибка настройки, покажите текст пользователю как есть
   (см. «Ошибки»). Fallback, если postMessage-RPC недоступен: откройте `trigger_url`
   навигацией в той же вкладке.

4. Опрашивайте прогресс снипетом `scripts/agent_poll.js` (замените `__JOB_ID__` на
   `'<jobId>'`), раз в 2-3 секунды, до `progress.status === 'done'`. Большие батчи
   выполняются волнами с дросселем и могут занимать десятки минут — читайте готовые единицы
   по мере появления, не дожидаясь всего батча. Если вернулся `setupError` — прекратите опрос
   и покажите пользователю `setupError.message` как есть (см. «Ошибки»).

5. Прочитайте результаты снипетом `scripts/agent_read.js`: для 1-2 артикулов оставьте
   `__UNIT_IDS__ = null` — все тела вернутся одним вызовом (`readAll`); при батчах из многих
   артикулов передавайте в `__UNIT_IDS__` конкретные id порциями по 1-2 за вызов — полные
   тела большие. Результаты хранятся ограниченное время (до суток, старые завершённые могут
   вытесняться раньше) — забирайте их сразу. Юниты джобы:
   - `<jobId>__nm<nm>__detail` — ответ `cards/v4/detail`: `data.body.products` с ценой,
     рейтингом и остатками `sizes[].stocks[]`;
   - `<jobId>__nm<nm>__card` — контент `card.json`: склейка `colors`, описание,
     характеристики;
   - `<jobId>__local` — данные расширения: `storesData` (склады `wh` → имя, только
     встречающиеся в остатках товара) и `productCardDetail` (best-effort, может быть пустым).
     Появляется после завершения остальных единиц задания.

## Как отвечать пользователю

- **Не оставляйте сырые имена полей WB в ответе** — переводите их на русский; булевы
  значения показывайте как «да/нет». Минимальный глоссарий:

  | Поле WB | В ответе |
  | --- | --- |
  | `imt_id` | Склейка (ID склейки) |
  | `subjectId` | ID предмета |
  | `vendorCode` / Vendor code | Артикул поставщика |
  | `need_kiz` | Обязательная маркировка (да/нет) |
  | `time1`, `time2` (в `stocks`) | Время доставки — одно число `time1 + time2`, не сырые поля |

- **Источник истины** по цене/рейтингу/остаткам — юнит `__detail`. Данные `__local`
  дополняющие и best-effort: на первой загрузке могут быть пустыми, деградируйте молча
  (например, показывайте `wh <id>` вместо имени склада).
- **Таблица «по складам»**: просуммируйте `qty` из `products[].sizes[].stocks[]` по `wh`,
  имена складов возьмите из `local.storesData[wh].name`; время доставки склада — сумма
  `time1 + time2`.
- **Таблица «по размерам»**: сумма `qty` внутри каждого элемента `sizes[]`.
- **Склейка**: артикулы из `colors` юнита `__card`.
- Юнит со `status: 'done'`, но `data.ok === false` / `data.status === 0` — сетевая ошибка WB:
  сообщите об этом по конкретному артикулу и предложите повторить.
- Указывайте, что остатки — моментальный снимок на момент запроса.

## Ошибки

Ошибки настройки — задание не принято расширением. Приходят как текст ошибки `submit` либо в
`progress().setupError`; покажите `message` пользователю как есть:

| `code` | Что сделать пользователю |
| --- | --- |
| `extension_not_authenticated` | Открыть попап расширения e-Comet и ввести ключ расширения |
| `wb_not_authenticated` | Войти в аккаунт на wildberries.ru и повторить запрос |
| `extension_misconfigured` | Обновить/переустановить расширение |
| `token_rejected` | Запросить новый `browser_job` и повторить |

Ошибки уровня юнита (`status: 'error'`, поле `error`): `Agent job timed out` (нет ответа за
таймаут), `Fetch URL host is not allowed`, `Extension connection not ready` / `closed`
(расширение недоступно — попросите проверить установку). Снипет-ответ
`{ error: 'e-Comet extension did not respond' }` означает, что расширение не отвечает на
этой вкладке — проверьте, что открыта вкладка wildberries.ru и расширение включено.
